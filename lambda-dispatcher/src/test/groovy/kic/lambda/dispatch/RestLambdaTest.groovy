package kic.lambda.dispatch

class RestLambdaTest extends GroovyTestCase {

    void testApply() {
        def restLambda = new RestLambda("")
        // TODO we should have an add one lambda where we can test newState = currentState + event

    }

}
